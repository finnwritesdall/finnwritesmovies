---
eleventyExcludeFromCollections: true
layout: base.liquid
---
    <header>
      <h1 class="header-title">{{ title | upcase }}</h1>
    </header>
    <ul>
        {%- for post in collections.all -%}
            <li><a href="{{ post.url }}">{{ post.url }}</a></li>
        {%- endfor -%}
    </ul>
</div>